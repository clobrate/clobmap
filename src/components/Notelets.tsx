/**
 * Notelets — the notebook view of a document (notes-as-pages with a tree
 * sidebar). This is the Phase 0 scaffold: the view is selectable and
 * routable, but the notebook itself is built in later phases. See
 * docs/notelets/ for the plan.
 */
export function Notelets() {
  return (
    <div className="flex h-full items-center justify-center text-sm text-neutral-500">
      Notelets view — coming soon
    </div>
  );
}
